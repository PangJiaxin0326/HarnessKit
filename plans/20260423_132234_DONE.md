# HarnessKit Tool Result JSON Fix Plan

## Task Classification

- sequential
  - Inspect the existing cache layout and automatic tool loop to confirm where `provider-prompt` persistence and plain-text tool result prompting are implemented.
  - Remove the unused provider-prompt cache file logic with minimal public-surface churn.
  - Update automatic tool round trips so tool-assisted completions return validated structured JSON, with retry-on-invalid-JSON behavior.
  - Add or adjust Swift Testing coverage for the cache changes and structured tool-response behavior.
  - Verify the targeted test suite, then archive this plan when the work is complete.
- parallel
  - None. The runtime and tests touch the same execution path, so keeping this local avoids merge churn.

## Tasks

### 1. Inspect cache and tool loop seams

- status: DONE
- files touched: none
- checks run:
  - none
- remaining risks:
  - The current tool loop is prompt-driven, so the structured JSON contract needs to stay compatible with the existing `String`-based provider abstraction.

### 2. Remove unused provider-prompt cache logic

- status: DONE
- files touched:
  - `Sources/HarnessKit/HarnessTypes.swift`
  - `Sources/HarnessKit/FileSystemSupport.swift`
  - `Sources/HarnessKit/AIService.swift`
  - `README.md`
- checks run:
  - `swift test --filter HarnessKitTests`
  - `swift test`
- remaining risks:
  - Removing the cached prompt file changes the cache record surface, so downstream custom cache implementations may need to adopt the slimmer contract.

### 3. Return validated structured JSON for tool-assisted completions

- status: DONE
- files touched:
  - `Sources/HarnessKit/AIService.swift`
  - `Sources/HarnessKit/HarnessTypes.swift`
  - `README.md`
- checks run:
  - `swift test --filter HarnessKitTests`
  - `swift test`
- remaining risks:
  - Tool handlers still return `String`, so non-JSON tool payloads are surfaced as string output inside the final JSON envelope.

### 4. Add regression coverage and verify

- status: DONE
- files touched:
  - `Tests/HarnessKitTests/HarnessKitTests.swift`
- checks run:
  - `swift test --filter HarnessKitTests`
  - `swift test`
- remaining risks:
  - The retry coverage exercises one repair round; it does not assert the terminal failure path after repeated invalid JSON responses.

## Blockers

- None.

## Deferred

- None.
