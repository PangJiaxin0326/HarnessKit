# HarnessKit GPT-5.5 Inspection and Refactor Plan

## Task Classification

- sequential
  - Inspect `Docs/HarnessEngineering.md`, public runtime types, filesystem support, registries, tests, and current README.
  - Add focused regression tests for concrete bugs found during inspection.
  - Refactor implementation structure with minimal public-surface churn, preserving actor isolation and existing package dependencies.
  - Replace `README.md` with thorough documentation that explains the refactored harness model and maps it back to `Docs/HarnessEngineering.md`.
  - Run focused tests first, then full package verification, then archive this plan.
- parallel
  - A read-only `swift_explorer` architecture trace is running independently to cross-check bug candidates, access-level concerns, and test gaps.

## Tasks

### 1. Inspect architecture, docs, and current behavior

- status: DONE
- files touched:
  - `PLAN.md`
- checks run:
  - `swift test`
- remaining risks:
  - The first test pass is green, so fixes need targeted regression coverage rather than relying on existing tests.

### 2. Add regression coverage for discovered bugs

- status: DONE
- files touched:
  - `Tests/HarnessKitTests/HarnessKitTests.swift`
- checks run:
  - `swift test --filter subagentContextRoundTripPreservesNestedMarkdownAndClearsWaitingStatus` (expected failure before fix)
  - `swift test --filter skillFrontMatterTrimsYamlQuotesAndInlineListQuotes` (expected failure before fix)
- remaining risks:
  - The tests cover the observed parser/status bugs; broader API design cleanup still needs review.

### 3. Refactor runtime internals and bug fixes

- status: DONE
- files touched:
  - `Sources/HarnessKit/HarnessTypes.swift`
  - `Sources/HarnessKit/FileSystemSupport.swift`
  - `Sources/HarnessKit/AIService.swift`
  - `Sources/HarnessKit/Registries.swift`
  - `Tests/HarnessKitTests/HarnessKitTests.swift`
- checks run:
  - `swift test --filter subagentContextRoundTripPreservesNestedMarkdownAndClearsWaitingStatus`
  - `swift test --filter skillFrontMatterTrimsYamlQuotesAndInlineListQuotes`
  - `swift test --filter agentsReferencesLoadSkillsOnlyFromSkillsDirectoryAndDocumentsAsContext --filter automaticToolInvocationCanBeDeniedByPermissionChecker --filter streamInterceptsCodeFencedAutomaticToolCall --filter concurrentGenerationsKeepServiceWorkingUntilAllComplete`
  - `swift test`
- remaining risks:
  - AGENTS-linked reference document loading is filesystem-based and intentionally conservative; it does not implement a full Markdown/YAML parser.

### 4. Replace README documentation

- status: DONE
- files touched:
  - `README.md`
- checks run:
  - `swift test`
- remaining risks:
  - Documentation describes the current public runtime and default filesystem behavior; future provider integrations will need their own docs.

### 5. Verify, review, and archive

- status: DONE
- files touched:
  - `PLAN.md`
- checks run:
  - `swift test`
  - `swift_reviewer` review pass
  - `swift test` after review follow-ups
- remaining risks:
  - No blocking issues remain from review. The default AGENTS reference parser is intentionally lightweight and should stay covered by focused regression tests if expanded.

## Blockers

- None.

## Deferred

- None.
