# HarnessKit Abnormal Behavior Fix Plan

## Task Classification

- sequential
  - Inspect the current runtime, cache, and test seams for the reported abnormal behaviors.
  - Patch the runtime to execute automatic tool round trips, persist the provider prompt, and return to `.idle` after successful completion.
  - Add or update Swift Testing coverage for the new runtime and cache behavior.
  - Verify the package, archive the completed plan, and push the fix to GitHub.
- parallel
  - None. The changes touch the same runtime and test surface, so keeping the work local avoids merge churn.

## Tasks

### 1. Inspect the reported behaviors and current package seams

- status: DONE
- files touched: none
- checks run:
  - none
- remaining risks:
  - Tool-calling currently has no established wire format in the package, so the fix needs a small prompt-and-parser contract.

### 2. Patch runtime and cache behavior with minimal diffs

- status: DONE
- files touched:
  - `Sources/HarnessKit/AIService.swift`
  - `Sources/HarnessKit/HarnessTypes.swift`
  - `Sources/HarnessKit/FileSystemSupport.swift`
  - `README.md`
- checks run:
  - `swift test --filter HarnessKitTests`
  - `swift test`
- remaining risks:
  - Streaming preserves live chunking for normal answers, but responses that begin like a tool-call sentinel are buffered until the harness can disambiguate them.

### 3. Add regression coverage for tool execution, cache persistence, and idle recovery

- status: DONE
- files touched:
  - `Tests/HarnessKitTests/HarnessKitTests.swift`
- checks run:
  - `swift test --filter HarnessKitTests`
  - `swift test`
- remaining risks:
  - The new tests cover the default text protocol, not provider-native structured tool APIs.

### 4. Verify, archive, and publish the fix

- status: DONE
- files touched:
  - `PLAN.md`
- checks run:
  - `swift test --filter HarnessKitTests`
  - `swift test`
- remaining risks:
  - Git push still depends on remote authentication succeeding in this environment.

## Blockers

- None.

## Deferred

- A structured provider-native tool-calling API is out of scope for this pass; this fix will stay within the existing `String -> String` provider abstraction.
