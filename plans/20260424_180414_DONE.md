# HarnessKit Subagent Follow-Up Plan

## Task Classification

- sequential
  - Continue from the explorer/reviewer findings that were not fully handled in the first pass.
  - Add focused regression tests for malformed skill and subagent files.
  - Make invalid skill/subagent errors reachable without changing the public package shape unnecessarily.
  - Address the final reviewer findings around actor reentrancy, cancellation cleanup, symlink escapes, and marker-comment persistence.
  - Run targeted tests, then the full package suite.
  - Archive this plan when complete.
- parallel
  - None. The remaining changes are tightly coupled to filesystem parsing and tests.

## Tasks

### 1. Add malformed file regression tests

- status: DONE
- files touched:
  - `PLAN.md`
  - `Tests/HarnessKitTests/HarnessKitTests.swift`
- checks run:
  - `swift test --filter malformedSkillFrontMatterThrowsInvalidSkillFile --filter malformedSubagentDirectoryThrowsInvalidSubagent` (expected failure before fix)
- remaining risks:
  - Tests pin the two parser-error paths without broadening the public API.

### 2. Implement stricter invalid skill/subagent parsing

- status: DONE
- files touched:
  - `README.md`
  - `Sources/HarnessKit/AIService.swift`
  - `Sources/HarnessKit/FileSystemSupport.swift`
  - `Tests/HarnessKitTests/HarnessKitTests.swift`
- checks run:
  - `swift test --filter malformedSkillFrontMatterThrowsInvalidSkillFile --filter malformedSubagentDirectoryThrowsInvalidSubagent`
  - `swift test --filter agentsReferencesCannotEscapeWorkspace --filter satisfyPendingSubagentRequestsRestoresStatusAfterInvalidSubagent --filter duplicateConcurrentSubagentContextRequestsCreateOneWait`
  - `swift test --filter subagentIDTraversalIsRejected --filter satisfyPendingSubagentRequestsPropagatesCancellationWithoutRejectingRequest --filter concurrentSatisfyPendingSubagentRequestsStayWorkingUntilAllComplete`
  - `swift test --filter subagentFieldsRoundTripHarnessMarkerText --filter cancelledSubagentSatisfactionReconcilesAlreadyResolvedWaits --filter agentsReferencesCannotEscapeWorkspaceThroughSymlink`
  - `swift test --filter staleSubagentWaitRefreshDoesNotReopenSatisfiedWait`
  - `swift test --filter subagent --filter agentsReferences --filter concurrent --filter cancellation`
- remaining risks:
  - Skill front matter validation now rejects unterminated front matter; it remains intentionally permissive for simple scalar/list parsing. AGENTS reference loading is restricted to symlink-resolved workspace descendants. Subagent wait state is reconciled per subagent with generation guards instead of stale whole-set snapshots.

### 3. Verify and archive

- status: DONE
- files touched:
  - `PLAN.md`
  - `plans/20260424_180414_DONE.md`
- checks run:
  - `swift test`
  - `git diff --check`
- remaining risks:
  - Broad public API reshaping remains deferred to avoid breaking existing adopters without an explicit versioning plan.

## Blockers

- None.

## Deferred

- Broad public API reshaping remains deferred to avoid breaking existing adopters without an explicit versioning plan.
